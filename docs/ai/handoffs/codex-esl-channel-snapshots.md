HANDOFF: codex/esl-channel-snapshots · 2026-09-09
- objective: Advance the approved FreeSWITCH development sequence with a reusable scripted test peer and typed live-channel snapshots.
- state: implemented, mocked validation and independent review complete; draft PR delivery.
- changed: `models/channels.py` adds immutable typed rows, completeness/error states, strict identity/count validation and bounds; `clients/esl.py` adds the exact read-only JSON query and metadata; exports/README document the API. `tests/esl_peer.py` supports exact-command transcripts and fragmentation; snapshot/transcript tests cover real client behavior. Existing raw-command test now rejects XML rather than the newly supported exact JSON form.
- discovered fix: transcript tests reproduced socket leaks during greeting timeout and socket-option failure. `connect()` now closes the newly opened socket on setup/greeting exceptions, including failed context entry. Both new regression tests failed before this change.
- validation: missing snapshot module confirmed baseline has no API; targeted snapshot/transport tests passed after implementation. `PYTHONPATH=src /Users/dperez/git/UniqueOS/.venv/bin/python -m pytest -q --tb=short` — 234 passed. Ruff lint/format pass. No external server or DB used; Python 3.14 runtime.
- risks: medium, additive read API and connection cleanup. Complete response is not atomic lifecycle truth; callers must not treat snapshot absence as hangup. Rows are bounded at 10,000; malformed/refused results expose no partial rows or raw error text. One caller per ESL connection remains required.
- source: FreeSWITCH `mod_commands.c` show_function/show_as_json_callback; zero rows may omit `rows`, row_count is numeric. Implemented independently, no third-party code/dependency imported.
- review-fanout: stability-reviewer OK for snapshots and separate socket-cleanup follow-up; SDK-boundary-reviewer OK; migration n/a.
- BUGS: handshake socket cleanup corrected. MISSING TESTS: live UAT/server integration remains unverified. MIGRATION/DATA RISK: no migrations or persistent writes. NITS: none. Verdict: ship for review; human owns merge/deploy.
- next: land SDK and update UniqueOS seam/contracts; use typed results for app-owned reconciliation only after specifying races and partial-data handling. Continue disposable call-flow test design within existing infra gates. Do not add bgapi mutation replay or a second control-plane framework.
